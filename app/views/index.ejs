<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%= title %></title>
	</head>
	<body>
		<h1>Hello, <%= title %>!</h1>

		<ul>
			<% if (messages.length > 0) { %> <% messages.map(msg => { %>
			<li><%= msg.text %></li>
			<% })%> <% } else { %>
			<p>No message</p>
			<% } %>
		</ul>
	</body>
</html>
